# 工具規範

aiwff-mini 本體只做四件事：**靈魂、記憶、完整性、誠實紀律**。

其餘能力都由工具提供。工具各自獨立、各自安裝、壞了不會拖垮主腦。

---

## 一支工具要滿足什麼才算適配

### 1. 可以單獨裝，也可以單獨移除

一支工具是一個資料夾，裡面自帶安裝說明。裝它不需要先裝別的工具，移除它不會影響主腦或其他工具。

移除方式必須是一行指令，而且刪完不留殘骸。

### 2. 自己帶一份寫給 AI 看的安裝說明

不是寫給人看的 README，是**寫給替使用者安裝的那個 AI 看的規格書**。至少要有：

- **能力閘門** —— 讓 AI 先確認自己做不做得完；做不完就乾淨拒絕，不要裝一半
- **開發樹拒絕** —— 偵測到自己在工具原始碼目錄裡就停下，永遠不要改動作者自己的系統
- **先給看、等同意、才動手** —— 列出要建的檔案給人看，等他說好
- **驗收** —— 一組明確的通過條件，而且要能實跑
- **doctor** —— 裝完印出每項能力的狀態

### 3. 只增不改

只在自己的安裝目錄裡建檔。不改使用者的 shell profile、不動 PATH、不碰其他工具的檔案。

真的需要整合，就先問、先備份、再改，而且要講出來改了什麼。

### 4. doctor 要有四種狀態，不能只有好跟壞

| 狀態 | 意思 |
|---|---|
| `live ✅` | 這次真的跑過，確認可用 |
| `broken ❌` | 應該要動但沒動 —— **必須附一行可以直接複製貼上的修復指令** |
| `declined ⏸` | 使用者選擇不啟用 |
| `stale ⚠️` | 以前能用，但這次沒驗過 |

**`declined` 是合法的執行狀態，不是缺陷。** 使用者明白地選擇不裝某項能力，工具不該每次都跳出來煩他。

只有兩種狀態的健康檢查會把「使用者的決定」誤報成「系統故障」，那種紅燈久了就沒人看了。

### 5. 驗收條件必須「填錯的時候會失敗」

這條最容易做錯。

寫驗收條件時問自己一句：**「如果參數填錯了，這條會不會照樣通過？」**

會 → 這條沒有鑑別力，換一條。

實例：某支排程工具的驗收寫「檢查告警旗標為空」。但剛跑過的排程，週期參數填 1 小時或 24 小時都不會觸發告警——**兩種填法給出完全一樣的空旗標，填錯當下驗不出來，要等它真的停擺才爆。** 有鑑別力的做法是量「下次到期時間減去上次執行時間」，那個差值直接暴露真正吃進去的參數。

同理，要驗「某個數字降到 0」，動手前先量一次原本的值。原本就是 0 的話，事後你分不出來是修好了還是本來就沒事。

### 6. 檔案落點要能被找到

工具產出的東西，如果之後會被收割、歸檔或搬移，說明文件裡要涵蓋**所有合法落點**，不能只寫它剛產生時的那一格。

綁死原位的路徑會在搬移後撲空，然後把成功誤判成失敗。

### 7. 不准為了讓驗收通過而改工具

安裝過程中任何一項驗收沒過，就照實回報沒過並停下來。

**誠實的失敗是有用的結果；被偷偷修到通過的成功不是。**

---

## 目錄結構

```
tools/
└── <工具名>/
    ├── INSTALL.md          ← 寫給 AI 看的安裝規格
    ├── INSTALL.zh-TW.md    ← 繁中版（可選）
    └── payload/            ← 實際要裝的東西
```

---

## 現有工具

| 工具 | 做什麼 | 狀態 |
|---|---|---|
| `context-segments` | 讀對話歷史，算出哪些回合在吃你的 context window。唯讀，不會動你的歷史 | 已驗證可安裝 |

---

## 想貢獻一支工具

先讀上面七條，然後照 `tools/context-segments/` 的結構做一份。

送出前自己跑一次最關鍵的測試：**開一個全新的、沒有任何上下文的 AI，只給它你的 `INSTALL.md`，看它能不能自己裝起來並跑出正確結果。**

跑不通就是說明書的問題，不是那個 AI 笨。
